import React from "react";

export default function Header(){
    return(
        <header className="navbar">
      <nav className="nav-links">
        <a href="#">Home</a>
        <a href="#">Tasks</a>
        <a href="#">Explore</a>
        <a href="#">About</a>
      </nav>
    </header>
    )
}